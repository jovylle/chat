# Copy to keystore.properties and fill in. Used for release signing (AAB/APK).
# Keystore path is relative to android/app/ (../../ = repo root).
storePassword=your-store-password
keyPassword=your-key-password
keyAlias=android
storeFile=../../android.keystore
